@model IEnumerable<CMS.Data.Entities.OrderDetail>
<div class="container mt-4">
    <h2 class="text-info mb-3">CHI TI?T CÁC HÓA ÐON</h2>
    <table class="table table-bordered table-hover shadow-sm">
        <thead class="table-info text-dark">
            <tr>
                <th>ID</th>
                <th>Mã Ðon Ð?t</th>
                <th>Mã S?n Ph?m</th>
                <th>S? Lu?ng Mua</th>
                <th>Ðon Giá</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var od in Model)
            {
                <tr>
                    <td>@od.Id</td>
                    <td>Ðon hàng #@od.OrderId</td>
                    <td>S?n ph?m #@od.ProductId</td>
                    <td>@od.Quantity</td>
                    <td>@od.UnitPrice.ToString("#,##0") d</td>
                </tr>
            }
        </tbody>
    </table>
</div>
