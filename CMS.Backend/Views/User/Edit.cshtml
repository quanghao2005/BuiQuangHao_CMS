@model CMS.Data.Entities.User

<div class="container mt-4">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-warning text-dark">
            <h5 class="mb-0 fw-bold"><i class="bi bi-pencil-square"></i> CH?NH S?A NGU?I DÙNG</h5>
        </div>
        <div class="card-body">
            <form asp-action="Edit" method="post">
                @Html.AntiForgeryToken()

                <input type="hidden" asp-for="Id" />

                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Tên dang nh?p:</label>
                    <input asp-for="Username" class="form-control" readonly />
                    <small class="text-muted">Tên dang nh?p không th? thay d?i.</small>
                </div>
                <div class="mb-3">
                    <label class="form-label fw-bold">M?t kh?u m?i:</label>
                    <input type="password" asp-for="PasswordHash" class="form-control" required />
                    <span asp-validation-for="PasswordHash" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label class="form-label fw-bold">H? và tên:</label>
                    <input asp-for="FullName" class="form-control" required />
                    <span asp-validation-for="FullName" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label class="form-label fw-bold">Vai trò:</label>
                    <select asp-for="Role" class="form-control">
                        <option value="Administrator">Qu?n tr? viên</option>
                        <option value="Editor">Biên t?p viên</option>
                    </select>
                </div>

                <div class="d-flex justify-content-end">
                    <a asp-action="Index" class="btn btn-secondary me-2">Quay l?i</a>
                    <button type="submit" class="btn btn-warning text-dark fw-bold">C?p nh?t thông tin</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
